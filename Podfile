# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

platform :osx, '10.12.3'
target 'SmartGaitVideoMotion' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'CorePlot'
  pod 'SwiftCSV', :git => 'https://github.com/naoty/swiftcsv.git', :branch => 'develop'
  
  # Pods for SmartGaitVideoMotion

  target 'SmartGaitVideoMotionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SmartGaitVideoMotionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
